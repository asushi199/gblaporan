services:
  - type: web
    name: autoreport-kaunseling
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
      - key: GEMINI_API_KEY
        sync: false
